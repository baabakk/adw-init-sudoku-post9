// Re-export shared contract types for local convenience
import type {
  PuzzleRequest,
  PuzzleResponse,
  Difficulty,
  SudokuBoard,
  SudokuCell,
  SudokuRow,
} from "@init-sudoku-post9/contracts";

export type { PuzzleRequest, PuzzleResponse, Difficulty, SudokuBoard, SudokuCell, SudokuRow };
